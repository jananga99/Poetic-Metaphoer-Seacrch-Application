<!DOCTYPE html>
<html>

<head>
    <title>Poem Search App</title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <div class="container">
        <h1>Poem Search App</h1>
        <form action="/search" method="post">
            <select name="searchOption">
                <option value="title">Poem Title</option>
                <option value="content">Content</option>
                <option value="poet">Poet</option>
                <option value="metaphor">Metaphor</option>
                <option value="metaphorEnglish">Metaphor in English</option>
                <option value="meaning">Meaning</option>
            </select>
            <input type="text" name="searchTerm" placeholder="Search..." />
            <button type="submit">Search</button>
        </form>

        <% if (searchResults) { %>
            <h2>Search Results:</h2>
            <table>
                <tr>
                    <th>Poem Title</th>
                    <th>Content</th>
                    <th>Poet</th>
                    <th>Metaphor</th>
                    <th>Metaphor in English</th>
                    <th>Meaning</th>
                </tr>
                <% searchResults.forEach(result=> { %>
                    <tr>
                        <td>
                            <%= result.title %>
                        </td>
                        <td>
                            <%= result.content %>
                        </td>
                        <td>
                            <%= result.poet %>
                        </td>
                        <td>
                            <%= result.metaphor %>
                        </td>
                        <td>
                            <%= result.metaphorEnglish %>
                        </td>
                        <td>
                            <%= result.meaning %>
                        </td>
                    </tr>
                    <% }); %>
            </table>
            <% } %>

                <% if (error) { %>
                    <p class="error">
                        <%= error %>
                    </p>
                    <% } %>
    </div>
</body>

</html>